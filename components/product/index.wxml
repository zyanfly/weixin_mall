<view bind:tap="onTap" class="container">
    <view class="wrapper">
        <image mode="aspectFill" src="{{product.pic}}"></image>
        <view class='title'>{{product.name}}</view>
        <view class='author'>￥{{product.current_price}}</view>
    </view>
</view>