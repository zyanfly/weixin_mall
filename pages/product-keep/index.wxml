<!-- <block wx:for="{{products}}" wx:key="">
  <view bind:tap="tapProduct" data-id="{{item.id}}">
    <text>{{item.name}}</text>
    <text>{{item.current_price}}</text>
    <image class="pic" src="{{item.pic}}" />
  </view>
</block> -->
<view class="container">
  <view class="prod_wrapper" wx:for="{{products}}">
    <v-product class="product_item" wx:key="index" product="{{item}}" />
  </view>
</view>
<view wx:if="{{(products.length==0)}}" class="no-data">
  <text>您还没有收藏任何商品</text>
</view>
